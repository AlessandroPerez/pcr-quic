# Cargo configuration for pcr-quic workspace
# Suppress warnings during builds

[build]
# Suppress warnings from dependencies
rustflags = ["-A", "warnings"]

[term]
verbose = false
quiet = false

[profile.release]
# Optimize for performance
opt-level = 3
lto = true
codegen-units = 1
